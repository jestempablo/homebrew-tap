# jestempablo/homebrew-tap

Homebrew Cask for Pawel's Helpful Assistant:

```
brew install --cask jestempablo/tap/pawels-helpful-assistant
```

The Cask is declarative: name, version, pinned SHA-256 and the HTTPS URL of
the notarized release archive; no lifecycle scripts.
